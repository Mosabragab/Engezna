# 🤖 إصلاح وتطوير مساعد إنجزنا الذكي

---

## 🎯 الهدف الأساسي

> **تقديم تجربة طلب ذكية تُغني العميل تماماً عن إغلاق التطبيق والاتصال بالمطعم تليفونياً.**

المساعد الذكي هو **قلب تجربة العميل** في إنجزنا. يجب أن يكون:
- ذكياً ولبقاً
- سريع الفهم
- دقيقاً (لا يخترع معلومات)
- ودوداً بشخصية مصرية أصيلة

---

## 🚨 المشاكل الحالية (يجب إصلاحها)

| المشكلة | الوصف | الأولوية |
|---------|-------|----------|
| **عرض JSON خام** | الرد يظهر كـ JSON بدلاً من نص مُنسق للعميل | 🔴 حرجة |
| **الهلوسة** | يذكر مطاعم غير موجودة في قاعدة البيانات (مثل "مطعم الشيف") | 🔴 حرجة |
| **لا ترحيب** | لا يرحب بالعميل عند فتح المحادثة | 🔴 حرجة |
| **لا اسم العميل** | لا يستخدم اسم العميل في المحادثة | 🟡 مهمة |
| **ردود جافة** | بدون شخصية أو لباقة | 🟡 مهمة |
| **لا اقتراحات ذكية** | لا يقترح بدائل عند عدم توفر المطلوب | 🟡 مهمة |

---

## 📊 البيانات المتاحة في Supabase

### جدول providers (المتاجر):
```
الأعمدة المهمة:
- id (uuid)
- owner_id (uuid) 
- name_ar (text) - اسم المتجر بالعربي
- name_en (text) - اسم المتجر بالإنجليزي
- description (text)
- is_active (boolean)
- rating (decimal)
- city_id (uuid)
```

### المتاجر الفعلية حالياً:
| name_ar | name_en |
|---------|---------|
| لافندر كافيه | Lavender Cafe |
| مطعم الصفا | Al Safa Restaurant |
| سوبر ماركت النجاح | Al Najah Supermarket |
| سلطان بيتزا | Sultan Pizza |

### جدول products (المنتجات):
```
الأعمدة المهمة:
- id (uuid)
- provider_id (uuid)
- name_ar (text)
- name_en (text)
- description (text)
- price (decimal)
- category_id (uuid)
- is_available (boolean)
```

### جدول profiles (العملاء):
```
الأعمدة المهمة:
- id (uuid)
- full_name (text) - اسم العميل ⭐
- phone (text)
- email (text)
```

---

## ✅ المهام المطلوبة

---

### المهمة 1: إصلاح عرض الردود (لا JSON خام)

**المشكلة الحالية:**
الرد يظهر هكذا:
```json
{
  "text": "لقيتلك كفتة في بني سويف!...",
  "actions": ["show_products"],
  "suggestions": ["مطعم الصفا", "مطعم الشيف"]
}
```

**المطلوب:**
- الـ `text` يُعرض كنص عادي في فقاعة الرسالة
- الـ `actions` تُنفذ تلقائياً أو تُعرض كأزرار
- الـ `suggestions` تُعرض كأزرار سريعة (Quick Replies)

**ابحث عن:**
- Component المحادثة (Chat/Conversation)
- كيف يُعرض رد الـ AI حالياً
- أضف parsing للـ JSON قبل العرض

---

### المهمة 2: منع الهلوسة (لا اختراع معلومات)

**المشكلة الحالية:**
المساعد يذكر "مطعم الشيف" رغم أنه غير موجود في قاعدة البيانات!

**الحل المطلوب:**

**1. تعديل System Prompt للـ AI:**
```
قواعد صارمة:
- لا تذكر أي مطعم إلا إذا وجدته في نتائج البحث
- لا تخترع أسماء مطاعم أو منتجات
- إذا لم تجد المطلوب، قل "مش لاقي" واقترح بدائل من الموجود فعلاً
- كل معلومة (اسم، سعر، تقييم) يجب أن تأتي من قاعدة البيانات
```

**2. تعديل Function Calling:**
- يجب أن يبحث في قاعدة البيانات أولاً
- يستخدم فقط النتائج الفعلية
- لا يُكمل من خياله

**3. التحقق من الردود:**
- قبل إرسال الرد، تحقق أن كل مطعم مذكور موجود فعلاً
- إذا ذكر مطعم غير موجود، أزله من الرد

---

### المهمة 3: الترحيب باسم العميل

**المطلوب:**
عند فتح المحادثة، يجب:
1. جلب اسم العميل من جدول `profiles`
2. إرسال رسالة ترحيب تلقائية

**رسالة الترحيب:**
```
أهلاً [اسم العميل]! 👋

أنا مساعد إنجزنا الذكي، معاك عشان أساعدك تطلب أكلك المفضل بأسرع وقت.

ممكن تقولي عايز إيه النهارده؟ 🍔

💡 جرب تقول: "عايز بيتزا" أو "عندكو إيه من مطعم الصفا؟"
```

**إذا لم يوجد اسم:**
```
أهلاً بيك في إنجزنا! 👋

أنا مساعدك الذكي، هنا عشان أساعدك تطلب أكلك بسهولة.

عايز تطلب إيه النهارده؟ 🍔
```

**التنفيذ:**
- عند mount الـ Chat component، اجلب بيانات المستخدم
- أرسل رسالة ترحيب كأول رسالة من المساعد
- احفظ الاسم في state لاستخدامه لاحقاً

---

### المهمة 4: شخصية خدمة العملاء

**المطلوب:**
تحديث System Prompt ليشمل شخصية المساعد:

```
أنت "مساعد إنجزنا الذكي" - مساعد طلبات طعام ذكي ولبق.

🎭 شخصيتك:
- مصري أصيل، تتكلم باللهجة المصرية العامية
- ودود ومرح، لكن محترف
- صبور جداً، لا تتضايق من أي سؤال
- ذكي وسريع البديهة
- تحب تساعد الناس

🗣️ أسلوبك:
- استخدم "حضرتك" مع العملاء الجدد، ثم تدريجياً "أنت/انت"
- استخدم إيموجي بشكل معتدل (1-2 في كل رد)
- ردودك قصيرة ومباشرة (2-3 جمل كحد أقصى)
- اسأل سؤال واحد فقط في كل رد
- إذا العميل قال اسمه، استخدمه في المحادثة

📝 أمثلة على الردود الجيدة:
- "تمام يا [اسم]! لقيتلك بيتزا مارجريتا من سلطان بيتزا بـ 85 جنيه. تحبها؟ 🍕"
- "للأسف مش لاقي شاورما دلوقتي، بس عندي كفتة مشوية من مطعم الصفا. تجربها؟"
- "طلبك جاهز! 2 بيتزا + بيبسي = 180 جنيه. أضيفهم للسلة؟ 🛒"

❌ أمثلة على الردود السيئة:
- ردود طويلة جداً
- JSON أو كود
- "لا أستطيع" أو "غير متاح" بدون بديل
- نبرة رسمية أو جافة
- اختراع مطاعم أو منتجات غير موجودة
```

---

### المهمة 5: التعامل مع السيناريوهات

**السيناريو 1: طلب منتج موجود**
```
العميل: عايز بيتزا
المساعد: عندي بيتزا من سلطان بيتزا 🍕
- مارجريتا: 85 ج.م
- بيبروني: 95 ج.م  
- خضار: 80 ج.م
تحب أي واحدة؟
```

**السيناريو 2: طلب منتج غير موجود**
```
العميل: عايز سوشي
المساعد: للأسف مش لاقي سوشي دلوقتي في بني سويف 😅
بس عندي أكل بحري من مطعم الصفا لو حابب تجرب؟
```

**السيناريو 3: طلب من مطعم غير موجود**
```
العميل: عايز من مطعم الشيف
المساعد: مش لاقي مطعم اسمه "الشيف" عندنا 🤔
المطاعم المتاحة:
- مطعم الصفا
- سلطان بيتزا
- لافندر كافيه
- سوبر ماركت النجاح
تحب تطلب من أي واحد فيهم؟
```

**السيناريو 4: سؤال عن الأسعار**
```
العميل: الكفتة بكام؟
المساعد: الكفتة المشوية من مطعم الصفا بـ 100 ج.م ⭐ تقييمه 4.7
تحب أضيفها للسلة؟
```

**السيناريو 5: تأكيد الطلب**
```
العميل: تمام أضيفها
المساعد: تم إضافة كفتة مشوية للسلة! 🛒
المجموع: 100 ج.م
تحب حاجة تانية ولا نكمل الطلب؟
```

**السيناريو 6: سؤال خارج النطاق**
```
العميل: إيه الجو النهارده؟
المساعد: أنا متخصص في طلبات الأكل بس يا [اسم] 😄
قولي عايز تاكل إيه وأنا هساعدك!
```

**السيناريو 7: شكوى أو مشكلة**
```
العميل: الطلب اللي فات كان وحش
المساعد: آسف جداً على التجربة دي يا [اسم] 😔
ممكن تتواصل مع خدمة العملاء من صفحة "المساعدة" وهيساعدوك.
دلوقتي تحب تطلب حاجة تانية؟
```

---

### المهمة 6: واجهة المحادثة (UI/UX)

**المطلوب تحسينه:**

**1. فقاعات الرسائل:**
- رسائل المساعد: خلفية زرقاء فاتحة (#E3F2FD)
- رسائل العميل: خلفية رمادية (#F5F5F5)
- الوقت يظهر أسفل كل رسالة

**2. Quick Replies (الاقتراحات):**
- تظهر كأزرار أسفل رسالة المساعد
- عند الضغط، ترسل كرسالة من العميل
- تختفي بعد الضغط على أي منها

**3. أزرار المنتجات:**
- عند عرض منتجات، تظهر كـ cards صغيرة
- كل card فيها: صورة، اسم، سعر، زر "أضف"
- الضغط على "أضف" يضيف للسلة مباشرة

**4. حالة الكتابة:**
- عند انتظار رد الـ AI، اعرض "..." متحركة
- أو اعرض "المساعد بيكتب..."

**5. زر الميكروفون:**
- زر واضح للإدخال الصوتي
- عند الضغط، يتحول لأيقونة تسجيل
- بعد الانتهاء، يتحول النص ويُرسل

---

### المهمة 7: Functions للمساعد

**الـ Functions المطلوبة:**

**1. search_products(query, category?)**
```
يبحث في المنتجات حسب الاسم أو الفئة
يُرجع: قائمة المنتجات مع الأسعار والتقييمات
```

**2. search_providers(query)**
```
يبحث في المطاعم/المتاجر حسب الاسم
يُرجع: قائمة المتاجر المطابقة
```

**3. get_provider_menu(provider_id)**
```
يجلب قائمة طعام متجر معين
يُرجع: كل المنتجات مع الفئات والأسعار
```

**4. add_to_cart(product_id, quantity)**
```
يضيف منتج للسلة
يُرجع: تأكيد الإضافة والمجموع الجديد
```

**5. get_cart_summary()**
```
يجلب ملخص السلة الحالية
يُرجع: المنتجات والكميات والمجموع
```

**6. get_customer_name()**
```
يجلب اسم العميل من profiles
يُرجع: الاسم أو null
```

**7. get_available_categories()**
```
يجلب الفئات المتاحة (مطاعم، سوبر ماركت، إلخ)
يُرجع: قائمة الفئات
```

---

### المهمة 8: System Prompt الكامل

**اكتب System Prompt متكامل يشمل:**

```
أنت "مساعد إنجزنا الذكي" - مساعد طلبات طعام في تطبيق إنجزنا لتوصيل الطعام في بني سويف، مصر.

═══════════════════════════════════════
🎯 مهمتك الأساسية
═══════════════════════════════════════
مساعدة العملاء في طلب الطعام بطريقة سهلة وسريعة وممتعة.
هدفك أن تغنيهم تماماً عن الاتصال بالمطعم تليفونياً.

═══════════════════════════════════════
🚫 قواعد صارمة (لا تخالفها أبداً)
═══════════════════════════════════════
1. لا تذكر أي مطعم أو منتج إلا إذا وجدته في نتائج البحث
2. لا تخترع أسعار أو معلومات من خيالك
3. إذا لم تجد المطلوب، اعترف بذلك واقترح بدائل موجودة فعلاً
4. لا ترسل JSON أو كود للعميل - فقط نص طبيعي
5. لا تعطي معلومات طبية أو قانونية
6. لا تناقش مواضيع خارج نطاق طلب الطعام

═══════════════════════════════════════
🎭 شخصيتك
═══════════════════════════════════════
- مصري أصيل، لهجة مصرية عامية طبيعية
- ودود ومرح لكن محترف
- صبور جداً، لا تتضايق أبداً
- ذكي وسريع البديهة
- تحب تساعد الناس وتفرحهم

═══════════════════════════════════════
🗣️ أسلوب الكلام
═══════════════════════════════════════
- ردود قصيرة (2-3 جمل كحد أقصى)
- سؤال واحد فقط في كل رد
- إيموجي معتدل (1-2 لكل رد)
- استخدم اسم العميل إذا عرفته
- تجنب الكلمات الصعبة أو الفصحى

═══════════════════════════════════════
📍 معلومات مهمة
═══════════════════════════════════════
- المنطقة: بني سويف، مصر
- العملة: جنيه مصري (ج.م)
- التوصيل: يتم بواسطة المتجر نفسه
- الدفع: عند الاستلام (كاش)

═══════════════════════════════════════
🔧 كيف تتعامل مع الطلبات
═══════════════════════════════════════
1. افهم ماذا يريد العميل
2. ابحث في قاعدة البيانات (استخدم الـ functions)
3. اعرض النتائج بشكل واضح ومختصر
4. اسأل إذا يريد إضافة للسلة
5. أكد الإضافة واسأل عن المزيد

═══════════════════════════════════════
💬 أمثلة على الردود الصحيحة
═══════════════════════════════════════

طلب منتج موجود:
"عندي بيتزا من سلطان بيتزا 🍕 مارجريتا بـ 85 ج.م. تحبها؟"

منتج غير موجود:
"مش لاقي سوشي دلوقتي 😅 بس عندي أكل بحري من مطعم الصفا. تجربه؟"

مطعم غير موجود:
"مش لاقي المطعم ده عندنا 🤔 المتاح: الصفا، سلطان بيتزا، لافندر كافيه. تختار منهم؟"

تأكيد الإضافة:
"تمام! أضفت بيتزا مارجريتا للسلة 🛒 المجموع: 85 ج.م. حاجة تانية؟"

═══════════════════════════════════════
❌ أمثلة على الردود الخاطئة
═══════════════════════════════════════
- "{"text": "..."}" ← لا JSON أبداً
- "مطعم الشيف عنده..." ← لا تذكر مطعم غير موجود
- رد طويل أكثر من 4 سطور
- "لا أستطيع مساعدتك" بدون تقديم بديل
- نبرة رسمية أو فصحى

═══════════════════════════════════════
```

---

## 📁 الملفات المتوقع تعديلها

| الملف | التعديل |
|-------|---------|
| Chat Component | إصلاح عرض الردود + الترحيب |
| AI Service/Hook | تعديل System Prompt + Functions |
| Message Component | تنسيق الفقاعات + Quick Replies |
| API Route | التحقق من الردود قبل الإرسال |

---

## ✅ قائمة التحقق

### عرض الردود:
- [ ] لا يظهر JSON خام للعميل
- [ ] النص يظهر منسق في فقاعة
- [ ] الاقتراحات تظهر كأزرار
- [ ] المنتجات تظهر كـ cards

### منع الهلوسة:
- [ ] كل مطعم مذكور موجود في DB
- [ ] كل منتج مذكور موجود في DB
- [ ] الأسعار صحيحة من DB
- [ ] يعترف عند عدم وجود المطلوب

### الترحيب:
- [ ] رسالة ترحيب عند فتح المحادثة
- [ ] استخدام اسم العميل إذا متاح
- [ ] اقتراحات أولية للمساعدة

### الشخصية:
- [ ] لهجة مصرية طبيعية
- [ ] ردود قصيرة ومباشرة
- [ ] إيموجي معتدل
- [ ] ودود ولبق

### السيناريوهات:
- [ ] يتعامل مع طلب موجود
- [ ] يتعامل مع طلب غير موجود
- [ ] يتعامل مع مطعم غير موجود
- [ ] يتعامل مع أسئلة خارج النطاق

---

## 🆘 المساعدة

**إذا احتجت:**
- استعلام SQL للتحقق من البيانات
- توضيح أي نقطة
- اختبار سيناريو معين

**اطلب مني وسأساعدك!**

---

## 🎯 معايير النجاح

| المعيار | الهدف |
|---------|-------|
| دقة المعلومات | 100% (لا هلوسة) |
| رضا العميل | لا يحتاج يتصل بالمطعم |
| سرعة الرد | < 3 ثواني |
| فهم الطلب | > 95% من المرة الأولى |

---

**ابدأ الآن! 🚀**
