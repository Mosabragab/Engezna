# مساعد إنجزنا الذكي - AI Smart Assistant

> **حالة المشروع:** ✅ محدّث - تم إضافة GPT Intent Classification System

---

## نظرة عامة

مساعد ذكي للدردشة داخل تطبيق إنجزنا للتوصيل، يستخدم GPT-4o-mini مع OpenAI Function Calling لمساعدة العملاء في:
- اختيار الأقسام والمتاجر
- البحث عن المنتجات
- استعراض العروض
- إضافة المنتجات للسلة
- حذف/تعديل المنتجات من السلة

### الميزات الجديدة (15 ديسمبر 2025)
- **🧠 GPT Intent Classification**: استبدال كل Regex patterns بـ GPT لفهم النية (يفهم الأخطاء الإملائية واللهجات)
- **🗑️ Smart Remove Item**: حذف أصناف مع دعم الكمية ("امسح الزيت" vs "شيل واحدة بس")
- **🚚 Fixed Delivery Info**: استعلام التوصيل يستخدم `cart_provider_id` بشكل صحيح
- **❌ Removed Upselling**: إزالة اقتراحات الإضافة غير المنطقية

### الميزات السابقة (13 ديسمبر 2025)
- **🚀 Direct Payload Handlers**: معالجة أزرار الاختيار (category, provider, item, variant) مباشرة بدون GPT
- **💾 Memory System**: حفظ pending_item و pending_variant في الذاكرة لمتابعة السياق
- **🔢 Arabic Quantity Parser**: تحويل الأرقام العربية (واحد، اتنين، ٢) للكمية
- **⚡ Faster Response**: ردود فورية لأزرار الاختيار بدون استدعاء OpenAI
- **Intent Router**: نظام ذكي لاكتشاف نية المستخدم وإجبار استدعاء Tools
- **Quick Replies من DB**: توليد أزرار تلقائية من نتائج البحث
- **دعم أفضل للأسماء العربية**: البحث عن متاجر بالاسم مثل "الصفا"

---

## الملفات الرئيسية

| الملف | الوصف |
|-------|-------|
| `src/app/api/chat/route.ts` | API endpoint الرئيسي |
| `src/lib/ai/systemPrompt.ts` | System Prompt بالعربية المصرية |
| `src/lib/ai/tools.ts` | تعريف OpenAI Function Calling Tools |
| `src/lib/ai/normalizeArabic.ts` | تطبيع النص العربي للبحث |
| `src/lib/ai/intentClassifier.ts` | **جديد** - GPT-based Intent Classification |
| `src/lib/ai/intentHandlers.ts` | **جديد** - معالجات النوايا (cart, remove, delivery, etc.) |
| `src/lib/ai/responsePersonality.ts` | **جديد** - رسائل بشخصية مصرية |
| `src/hooks/useAIChat.ts` | React Hook للتفاعل مع API |
| `src/lib/store/chat.ts` | Zustand Store لحفظ المحادثات |
| `src/lib/store/cart.ts` | Zustand Store للسلة (مع `removeItemCompletely`) |
| `src/components/customer/voice/SmartAssistant.tsx` | UI Component |

---

## 🧠 نظام GPT Intent Classification (جديد - 15 ديسمبر 2025)

### المشكلة
- كان النظام يستخدم Regex patterns لفهم نية المستخدم
- كل خطأ إملائي يحتاج regex جديد (مستحيل لـ 50 مليون مستخدم!)
- "امس" vs "امسح" vs "امساح" - كلهم نفس المعنى

### الحل
استبدال **كل** الـ Regex patterns بـ GPT-4o-mini للتصنيف:

```typescript
// ملف: src/lib/ai/intentClassifier.ts

export type IntentType =
  | 'search_product'      // البحث عن منتج
  | 'cart_inquiry'        // استفسار عن السلة
  | 'remove_item'         // حذف صنف من السلة
  | 'clear_cart'          // مسح السلة كلها
  | 'cancel'              // إلغاء/رجوع
  | 'delivery_info'       // استفسار عن التوصيل
  | 'confirm'             // تأكيد
  | 'quantity_response'   // رد بالكمية
  | 'go_to_cart'          // الذهاب للسلة
  | 'greeting'            // تحية
  | 'thanks'              // شكر
  | 'help'                // مساعدة
  | 'unknown'             // غير معروف
```

### الكيانات المستخرجة (Entities)
```typescript
entities: {
  product_name?: string   // اسم المنتج للبحث أو الحذف
  quantity?: number       // الكمية (لو ذكرها المستخدم)
  provider_name?: string  // اسم المتجر
}
```

### مثال
```
المستخدم: "شيل واحدة بيتزا بس"

GPT يفهم:
- intent: "remove_item"
- entities: { product_name: "بيتزا", quantity: 1 }

النتيجة: يشيل 1 بيتزا بس، مش كلهم!
```

### المعالجات (Handlers)

| Handler | الوصف |
|---------|-------|
| `handleCartInquiry()` | عرض محتويات السلة |
| `handleClearCart()` | مسح السلة كلها |
| `handleRemoveItem()` | حذف صنف (مع دعم الكمية) |
| `handleCancel()` | إلغاء/رجوع ذكي حسب السياق |
| `handleDeliveryInfo()` | معلومات التوصيل (يستخدم cart_provider_id) |
| `handleGreeting()` | تحية حسب الوقت |
| `handleThanks()` | رد على الشكر |
| `handleGoToCart()` | الذهاب للسلة |

### Cart Actions المدعومة

| Action | الوصف |
|--------|-------|
| `ADD_ITEM` | إضافة صنف للسلة |
| `CLEAR_AND_ADD` | مسح السلة ثم إضافة |
| `CLEAR_CART` | مسح السلة كلها |
| `REMOVE_ITEM` | حذف صنف (مع دعم quantity للحذف الجزئي) |

---

## الميزات المُنفَّذة (جلسات سابقة)

### 1. تطبيع النص العربي (Arabic Normalization)

**ملف:** `src/lib/ai/normalizeArabic.ts`

يحل مشكلة البحث عن كلمات مثل "كفتة" و "كفته" بنتائج مختلفة.

**التحويلات:**
- `ة → ه` (تاء مربوطة)
- `ى → ي` (ألف مقصورة)
- `أ, إ, آ → ا` (أنواع الألف)
- `ؤ → و`, `ئ → ي` (الهمزة)
- إزالة التشكيل (الفتحة، الضمة، الكسرة...)

**مثال:**
```typescript
normalizeArabic("كفتة") // → "كفته"
normalizeArabic("ملوخيّة") // → "ملوخيه"
```

### 2. اختيار المتجر بالاسم (Provider Name Resolution)

**المشكلة:** المستخدم يقول "من سلطان البيتزا" والنظام يحتاج UUID.

**الحل:** دالة `resolveProviderByName()` تحول الاسم إلى UUID تلقائياً.

**يعمل في:**
- `get_provider_menu`
- `search_in_provider`
- `get_promotions`

### 3. عرض العروض مع الأصناف المحددة

**المشكلة:** العروض التي `applies_to = 'specific'` لم تُظهر الأصناف المتأثرة.

**الحل:**
- جلب `product_ids` من العرض
- جلب تفاصيل الأصناف من `menu_items`
- إضافتها كـ `affected_products` في الرد

### 4. إصلاح عرض الأسعار

**المشكلة:** المساعد كان يقول "مش قادر أقول الأسعار" رغم وجودها في البيانات.

**الحل:** قاعدة صريحة في System Prompt:
> "لو السعر موجود في نتيجة Tool Call — لازم تقوله للمستخدم بالأرقام"

### 5. حفظ القسم المختار (selected_category)

**المشكلة:** المساعد كان يسأل عن القسم مرة أخرى بعد اختياره.

**الحل:**
- إضافة `selectedCategory` في Zustand Store
- إرسالها مع كل طلب API
- قاعدة في System Prompt: "لو selected_category موجود: لا تسأل عن القسم"

### 6. Intent Router (Zapier-style)

**المشكلة:** المساعد كان يرد أحياناً بدون استدعاء Tools.

**الحل:** نظام Intent Router يحلل رسالة المستخدم ويجبر استدعاء Tool معين:

| النية | الكلمات المفتاحية | Tool المُجبَر |
|-------|------------------|--------------|
| عروض | عروض، خصم، promotions | `get_promotions` |
| تحية | هاي، السلام، أهلا | `get_available_categories_in_city` |
| بحث منتج | عايز، عاوز، موجود | `search_in_provider` أو `search_product_in_city` |
| اختيار متجر | من X، عند X، مطعم X | `search_providers` |

**مثال:**
```typescript
// المستخدم: "الصفا"
// → Intent: provider_selection
// → Forced Tool: search_providers
// → Args: { query: "الصفا" }
```

### 7. توليد Quick Replies من نتائج DB

**المشكلة:** الأزرار لم تكن تُعرض للمستخدم بعد البحث.

**الحل:** دالة `generateQuickRepliesFromToolResults()` تحول نتائج DB لأزرار:

| Tool | نوع الأزرار |
|------|------------|
| `search_providers` | أزرار متاجر `provider:<id>` |
| `get_provider_menu` | أزرار أصناف `item:<id>` مع السعر |
| `get_promotions` | أزرار العروض + الأصناف المتأثرة |
| `get_item_variants` | أزرار الأحجام `variant:<id>` مع السعر |

---

## Tools المتاحة (OpenAI Function Calling)

| Tool | الوصف |
|------|-------|
| `get_customer_name` | جلب اسم العميل للترحيب |
| `get_available_categories_in_city` | الأقسام المتاحة في المدينة |
| `search_providers` | البحث عن متاجر (بالقسم أو الاسم) |
| `get_provider_menu` | قائمة منتجات متجر |
| `search_in_provider` | البحث داخل متجر |
| `get_menu_item_details` | تفاصيل منتج |
| `get_item_variants` | أحجام/اختيارات المنتج |
| `get_variant_details` | تفاصيل اختيار |
| `get_promotions` | العروض النشطة |
| `search_product_in_city` | البحث عن منتج في كل المتاجر |

---

## Payloads المدعومة

| Payload | الوصف | مثال |
|---------|-------|------|
| `category:<code>` | اختيار قسم | `category:restaurant_cafe` |
| `provider:<uuid>` | اختيار متجر | `provider:abc123-...` |
| `item:<uuid>` | اختيار منتج | `item:xyz789-...` |
| `variant:<uuid>` | اختيار حجم/نوع | `variant:def456-...` |

---

## هيكل API Request

```typescript
interface ChatAPIRequest {
  messages: Array<{ role: 'user' | 'assistant'; content: string }>
  customer_id?: string
  city_id: string // إلزامي
  selected_provider_id?: string
  selected_provider_category?: string
  selected_category?: string // جديد - القسم المختار
  memory?: Record<string, unknown>
}
```

---

## هيكل API Response

```typescript
interface ChatAPIResponse {
  reply: string
  quick_replies?: Array<{ title: string; payload: string }>
  cart_action?: {
    type: 'ADD_ITEM'
    provider_id: string
    menu_item_id: string
    menu_item_name_ar: string
    quantity: number
    unit_price: number
    variant_id?: string
    variant_name_ar?: string
  }
  selected_provider_id?: string
  selected_provider_category?: string
  selected_category?: string
  memory?: Record<string, unknown>
}
```

---

## قواعد مكافحة الهلوسة (Anti-Hallucination)

1. **لا تدعي وجود شيء** إلا إذا رجع من Tool Call
2. **لا تؤلف أسماء** متاجر أو منتجات
3. **لا تذكر أسعار** إلا من DB
4. **لو النتيجة فارغة** → قل "مش لاقي نتايج"
5. **لو السعر موجود** → لازم تقوله (ممنوع "مش عارف")

---

## Logging

الـ Console يُظهر:
- `🎯 [INTENT ROUTER]` - تحليل نية المستخدم
- `🎯 [INTENT]` - النية المكتشفة
- `🔍 [AI TOOL CALL]` - بداية استدعاء tool
- `📦 [AI RESULT]` - نتيجة الاستدعاء
- `🔤 [NORM FILTER]` - فلترة التطبيع العربي
- `🚨 [AI EMPTY]` - نتيجة فارغة
- `✅ [PROVIDER RESOLVE]` - نجاح تحويل اسم لـ UUID
- `🔘 [QUICK REPLIES]` - توليد أزرار من النتائج

---

## المهام المتبقية (للجلسة القادمة)

- [ ] اختبار شامل للسيناريوهات في Production
- [ ] إضافة caching للنتائج المتكررة
- [ ] تحسين أداء البحث للمدن الكبيرة

---

## نظام Direct Payload Handlers (جديد)

### كيف يعمل؟

بدلاً من إرسال كل رسالة لـ GPT، النظام الجديد يعالج الأزرار مباشرة:

```
User clicks "حواوشي لحمة (55 ج.م)" → item:abc123
                                          ↓
                               Direct Handler (NO GPT)
                                          ↓
                           Get item details + variants
                                          ↓
                        Show variant buttons + save to memory
```

### Payloads المدعومة

| Payload | Handler | الوصف |
|---------|---------|-------|
| `category:xxx` | `handleCategoryPayload()` | عرض المتاجر في القسم |
| `provider:xxx` | `handleProviderPayload()` | عرض منيو المتجر |
| `item:xxx` | `handleItemPayload()` | عرض variants أو سؤال الكمية |
| `variant:xxx` | `handleVariantPayload()` | سؤال الكمية |
| `qty:x` | `handleQuantityInput()` | إضافة للسلة |

### Memory System

```typescript
interface ChatMemory {
  pending_item?: {
    id: string
    name_ar: string
    price: number
    provider_id: string
    provider_name_ar?: string
    has_variants?: boolean
  }
  pending_variant?: {
    id: string
    name_ar: string
    price: number
  }
  awaiting_quantity?: boolean
}
```

### Arabic Quantity Parser

يدعم تحويل:
- أرقام إنجليزية: `1`, `2`, `3`...
- أرقام عربية: `١`, `٢`, `٣`...
- كلمات: `واحد`, `اتنين`, `تلاته`...
- كلمات شائعة: `عادي` = 1

---

## سيناريوهات الاختبار (Acceptance Tests)

| # | المدخل | النتيجة المتوقعة | Status |
|---|--------|-----------------|--------|
| 1 | "عايز كفته" / "كفتة" | نفس النتائج (تطبيع عربي) | ✅ |
| 2 | "من سلطان البيتزا" | تحويل الاسم لـ UUID + عرض المنيو | ✅ |
| 3 | "الصفا" | البحث عن المتجر + عرض أزرار | ✅ |
| 4 | "قولي اسعارهم" | عرض الأسعار بـ ج.م | ✅ |
| 5 | "فيه عروض؟" | عرض العروض + الأصناف المتأثرة كأزرار | ✅ |
| 6 | "عايز بيتزا" | عرض المتاجر كأزرار (مش قائمة) | ✅ |
| 7 | بعد اختيار القسم | لا يسأل عن القسم مرة أخرى | ✅ |
| 8 | "هاي" أو "السلام عليكم" | ترحيب + أزرار الأقسام | ✅ |

---

## ملاحظات للمطور

1. **city_id إلزامي** - بدونه يرجع رسالة "اختار المدينة"
2. **UUID Validation** - كل IDs يتم التحقق منها قبل الاستخدام
3. **Rate Limiting** - 100 رسالة / 24 ساعة لكل user
4. **Temperature** - 0.7 للردود الطبيعية

---

*آخر تحديث: 15 ديسمبر 2025 - تم إضافة GPT Intent Classification System وإزالة Regex patterns*
